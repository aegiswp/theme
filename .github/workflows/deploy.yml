name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Add your deployment steps here, e.g., FTP, SSH, rsync, GitHub Pages, etc.
      - name: Deploy placeholder
        run: echo "Add deployment steps here."
