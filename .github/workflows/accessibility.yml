# yaml-language-server: $schema=https://json.schemastore.org/github-workflow

name: Accessibility Check

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  accessibility-check:
    runs-on: ubuntu-latest
    env:
      DB_HOST: 127.0.0.1
      DB_NAME: wordpress
      DB_USER: root
      DB_PASSWORD: root_password_ci
      WP_ADMIN_USER: admin
      WP_ADMIN_PASSWORD: admin_password_ci
      WP_ADMIN_EMAIL: ci@example.com
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password_ci
          MYSQL_DATABASE: wordpress
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Node dependencies
        run: npm ci
<<<<<<< Updated upstream

      - name: Install pa11y-ci
        run: npm install -g pa11y-ci
=======
>>>>>>> Stashed changes

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          tools: composer, wp-cli

      - name: Install Composer dependencies
<<<<<<< Updated upstream
        run: composer install --prefer-dist --no-progress --no-dev

      - name: Build theme
        run: npm run build --if-present
=======
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: Build theme
        run: npm run build
>>>>>>> Stashed changes

      - name: Set up WordPress
        run: |
          mkdir -p wp
          wp core download --path=wp
          wp config create --path=wp --dbname=${{ env.DB_NAME }} --dbuser=${{ env.DB_USER }} --dbpass=${{ env.DB_PASSWORD }} --dbhost=${{ env.DB_HOST }} --force
<<<<<<< Updated upstream
          # Database is already created by the MySQL service container (MYSQL_DATABASE).
=======
>>>>>>> Stashed changes
          wp db check --path=wp
          wp core install --path=wp --url=http://localhost:8080 --title="Aegis Test" --admin_user=${{ env.WP_ADMIN_USER }} --admin_password=${{ env.WP_ADMIN_PASSWORD }} --admin_email=${{ env.WP_ADMIN_EMAIL }} --skip-email
          ln -sfn "$GITHUB_WORKSPACE" wp/wp-content/themes/aegis
          wp theme activate aegis --path=wp

      - name: Start PHP server
        run: php -S localhost:8080 -t wp > /dev/null 2>&1 &

      - name: Wait for server to start
        run: |
          for i in {1..30}; do
            if curl -f http://localhost:8080 > /dev/null 2>&1; then
              echo "✓ Server is ready"
              exit 0
            fi
            echo "Waiting for server... ($i/30)"
            sleep 1
          done
          echo "✗ Server failed to start"
          exit 1

      - name: Run accessibility scan
        run: npx pa11y-ci --config .pa11yci.json --threshold 10
