---
title: "Typography"
description: "Fluid typography system with 22 font size presets and variable fonts"
---

## Overview

Aegis features a comprehensive fluid typography system that scales intelligently across viewport sizes using CSS `clamp()` functions.

## Font Families

Aegis includes two variable fonts:

| Name | Slug | Font Stack | Weight Range |
|------|------|------------|--------------|
| Lexend | `lexend` | `Lexend, sans-serif` | 300–900 |
| JetBrains | `jetbrains` | `JetBrains, monospace` | 100–900 |

<Note>
Fonts are loaded conditionally based on your selections in the Site Editor. If you don't use a font, it won't be loaded.
</Note>

### Default Usage

- **Body text**: Lexend
- **Headings**: Lexend
- **Code blocks**: JetBrains

## Font Sizes

Aegis provides 22 fluid font size presets:

| Name | Slug | Size | Fluid Range |
|------|------|------|-------------|
| 96px | `96` | `clamp(80px, 9.6vw, 96px)` | 80px → 96px |
| 88px | `88` | `clamp(72px, 8.8vw, 88px)` | 72px → 88px |
| 80px | `80` | `clamp(64px, 8vw, 80px)` | 64px → 80px |
| 72px | `72` | `clamp(56px, 7.2vw, 72px)` | 56px → 72px |
| 64px | `64` | `clamp(52px, 6.4vw, 64px)` | 52px → 64px |
| 60px | `60` | `clamp(48px, 6vw, 60px)` | 48px → 60px |
| 52px | `52` | `clamp(44px, 5.2vw, 52px)` | 44px → 52px |
| 48px | `48` | `clamp(40px, 4.8vw, 48px)` | 40px → 48px |
| 44px | `44` | `clamp(40px, 4.4vw, 44px)` | 40px → 44px |
| 40px | `40` | `clamp(36px, 4vw, 40px)` | 36px → 40px |
| 36px | `36` | `clamp(32px, 3.6vw, 36px)` | 32px → 36px |
| 32px | `32` | `clamp(28px, 3.2vw, 32px)` | 28px → 32px |
| 28px | `28` | `clamp(24px, 2.8vw, 28px)` | 24px → 28px |
| 24px | `24` | `clamp(22px, 2.4vw, 24px)` | 22px → 24px |
| 22px | `22` | `clamp(20px, 2.2vw, 22px)` | 20px → 22px |
| 20px | `20` | `clamp(19px, 2vw, 20px)` | 19px → 20px |
| 18px | `18` | `clamp(17px, 1.8vw, 18px)` | 17px → 18px |
| 16px | `16` | `clamp(16px, 1.6vw, 16px)` | 16px |
| 14px | `14` | `clamp(14px, 1.4vw, 14px)` | 14px |
| 12px | `12` | `clamp(12px, 1.2vw, 12px)` | 12px |
| 10px | `10` | `clamp(10px, 1vw, 10px)` | 10px |
| 8px | `8` | `clamp(8px, 0.8vw, 8px)` | 8px |
| Inherit | `inherit` | `inherit` | — |

## Font Weights

Custom font weight tokens are available:

| Name | CSS Variable | Value |
|------|--------------|-------|
| Black | `--wp--custom--font-weight--black` | 900 |
| Extra Bold | `--wp--custom--font-weight--extra-bold` | 800 |
| Bold | `--wp--custom--font-weight--bold` | 700 |
| Semi Bold | `--wp--custom--font-weight--semi-bold` | 600 |
| Medium | `--wp--custom--font-weight--medium` | 500 |
| Regular | `--wp--custom--font-weight--regular` | 400 |
| Light | `--wp--custom--font-weight--light` | 300 |
| Extra Light | `--wp--custom--font-weight--extra-light` | 200 |
| Thin | `--wp--custom--font-weight--thin` | 100 |

## Default Heading Styles

| Element | Font Size | Line Height |
|---------|-----------|-------------|
| H1 | 44px | 52px |
| H2 | 36px | 44px |
| H3 | 32px | 40px |
| H4 | 28px | 36px |
| H5 | 24px | 32px |
| H6 | 20px | 28px |

## Usage in CSS

```css
.my-element {
  font-family: var(--wp--preset--font-family--lexend);
  font-size: var(--wp--preset--font-size--18);
  font-weight: var(--wp--custom--font-weight--medium);
  line-height: 1.6;
}

code {
  font-family: var(--wp--preset--font-family--jetbrains);
}
```

## Typography Settings

Aegis enables these typography controls in the block editor:

- **Custom font sizes** - Enter any size value
- **Line height** - Adjust line spacing
- **Drop cap** - Large first letter for paragraphs
- **Writing mode** - Vertical text support

<Tip>
Use the fluid font sizes for responsive typography that scales smoothly across all screen sizes without media queries.
</Tip>
